apply plugin: 'findbugs'
apply plugin: 'pmd'

tasks.withType(FindBugs) {
    reports {
        xml.enabled = false
        html.enabled = true
    }
 }
 
findbugs {
    ignoreFailures = true
}

pmd{
    ignoreFailures = true
} 
